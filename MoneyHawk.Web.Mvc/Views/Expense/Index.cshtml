@using MoneyHawk.Core
@using Net.Web
@model IEnumerable<PurchaseInvoice>

@{
    ViewBag.Title = "Expenses";
}

@{
    var grid = new WebGrid<PurchaseInvoice>(Model)
    {
       
    
    };
}

<div id="grid">
    @grid.GetHtml(tableStyle:"table table-striped table-bordered")
</div>